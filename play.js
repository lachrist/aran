var p = new Proxy({a:1}, {deleteProperty: function (o, p) { console.log(p); return false }})


delete x >>> 
  
  (aran.erase = )

  (aran.del = false, aran.push(delete x), aran.del ? (aran.pop(), aran.delresult) : aran.traps.erase("x", aran.pop()))

  (aran.delete = aran.traps.Primitive(true), delete x, aran.delete)