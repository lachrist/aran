export const add = (x, y) => x + y;

export const multiply = (x, y) => x * y;

export const devides = (x, y) => x % y === 0;

export const append = (s1, s2) => `${s1}${s2}`;

export const equals = (x, y) => x === y;
