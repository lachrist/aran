/**
 * @type {<S>(
 *   node: estree.Program,
 *   context: import("./context.js").Context<S>,
 *   options: { kind: aran.ProgramKind },
 * ) => aran.Program<unbuild.Atom<S>>}
 */
export const unbuildProgram = (node, context, { kind }) => TODO;
