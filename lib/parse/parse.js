"use strict";

export.parse = (code, source, parser) => {
  if (source.type === "")
};

exports.parseEval = (code, strict, mode, parser) => {};

exports.parseScript = (code, parser) => parser.parse(code, {sourceType:})

exports.parseModule = (code, parser) => {};
